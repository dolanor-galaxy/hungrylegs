{
  "import": "import",

  "database": {
    "driver": "postgres",
    "connection": "host=db port=5432 user=postgres dbname=postgres password=postgres sslmode=disable",
    "post": [
      "set search_path='{athlete}'"
    ]
  }
}
